# SKILLS-GAP-ANALYZER
his keeps your code simple enough for you to understand, while making the output look much cleaner.  One more improvement I'd make: format the Pandas table yourself rather than simply doing print(df). That will make V1 look much more like an actual project instead of raw DataFrame output.
